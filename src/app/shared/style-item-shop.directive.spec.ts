import { StyleItemShopDirective } from './style-item-shop.directive';

describe('StyleItemShopDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleItemShopDirective();
    expect(directive).toBeTruthy();
  });
});
