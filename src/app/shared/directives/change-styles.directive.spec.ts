import { ChangeStylesDirective } from './change-styles.directive';

describe('ChangeStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeStylesDirective();
    expect(directive).toBeTruthy();
  });
});
